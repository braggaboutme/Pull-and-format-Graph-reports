# Pull-and-format-Graph-reports

This script pulls all reports from the reporting API in Graph using the "delegated" permission model. It then outputs and formats all of the json output into a csv format.
